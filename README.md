# agentic-shot
An agentic pipeline that turns a one-line scene description into a video shot. An LLM writes a shot spec, generates a keyframe, critiques its own output field by field, and revises until it matches.
